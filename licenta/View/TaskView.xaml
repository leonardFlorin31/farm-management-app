<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="licenta.View.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:licenta.ViewModel"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <!-- Resurse locale -->
    <UserControl.Resources>
        <!-- Converter pentru Visibility (bool → Visibility) -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:TaskViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Icon + Titlu -->
        <fa:IconImage Icon="Clipboard"
                      Style="{StaticResource menuButtonIcon}"
                      Foreground="{StaticResource color4}"
                      Margin="0,0,10,0"
                      HorizontalAlignment="Left"
                      Grid.Row="0" Grid.Column="0"/>
        <TextBlock 
                   Text="Sarcini"
                   Margin="30,5,0,0"
                   HorizontalAlignment="Left"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{StaticResource color4}"/>

        <!-- Zona de creare task: vizibilă doar pentru manager/admin -->
        <Border Grid.Row="1"
                Grid.ColumnSpan="2"
                Padding="8"
                Background="#F5F5F5"
                CornerRadius="4"
                Visibility="{Binding CanCreateTasks, Converter={StaticResource BoolToVis}}"
                Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBox Width="250"
                         Margin="0,0,10,0"
                         mah:TextBoxHelper.Watermark="Titlu nou sarcina"
                         Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}"/>
                <ComboBox Width="150"
                          ItemsSource="{Binding Employees}"
                          mah:TextBoxHelper.Watermark="Angajat"
                          Margin="0,0,10,0"
                          SelectedItem="{Binding Employee}"/>
                <ComboBox Width="150"
                          ItemsSource="{Binding StatusValues}"
                          mah:TextBoxHelper.Watermark="Status"
                          SelectedItem="{Binding NewTaskStatus}"/>
                <TextBox Width="250"
                         Margin="10,0,0,0"
                         mah:TextBoxHelper.Watermark="Descriere sarcina"
                         Text="{Binding NewTaskDescription, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Adaugă task"
                        Margin="10,0,0,0"
                        Background="{StaticResource color13}"
                        Foreground="White"
                        Command="{Binding CreateTaskCmd}"
                        IsEnabled="{Binding CanCreateTasks}"/>
                        
                <Button Content="Șterge task"
                        Margin="5,0,0,0"
                        Command="{Binding DeleteTaskCmd}"
                        IsEnabled="{Binding CanCreateTasks}"
                        Background="{StaticResource color14}" 
                        Foreground="White"/>
                
            </StackPanel>
        </Border>
        
        <Border Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,10" Padding="8" Background="#F9F9F9" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Filtrează:" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>

                <TextBox Width="200"
                         Margin="0,0,10,0"
                         mah:TextBoxHelper.Watermark="După titlu..."
                         Text="{Binding FilterTitle, UpdateSourceTrigger=PropertyChanged}"/>

                <ComboBox Width="150"
                          Margin="0,0,10,0"
                          mah:TextBoxHelper.Watermark="După angajat..."
                          ItemsSource="{Binding Employees}"
                          SelectedItem="{Binding FilterEmployee, Mode=TwoWay}"/>

                <ComboBox Width="150"
                          mah:TextBoxHelper.Watermark="După status..."
                          ItemsSource="{Binding StatusValues}"
                          SelectedItem="{Binding FilterStatus, Mode=TwoWay}"/>
                <Button Content="Resetează"
                        Margin="10,0,0,0"
                        Background="{StaticResource color13}"
                        Foreground="White"
                        Command="{Binding ResetFiltersCmd}"/>
            </StackPanel>
        </Border>

        <!-- Lista de task-uri -->
        <Border Margin="10,10,20,10"
                Grid.Row="3"
                Padding="10"
                CornerRadius="8"
                BorderThickness="2"
                Background="White"
                BorderBrush="{DynamicResource AccentColorBrush}">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource MahApps.Colors.Gray1}"
                                  BlurRadius="8"
                                  ShadowDepth="2"
                                  Opacity="0.5"/>
            </Border.Effect>
        <ListView Grid.Row="3"
                  Grid.ColumnSpan="2"
                  ItemsSource="{Binding Tasks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Titlu"
                                    DisplayMemberBinding="{Binding Title}"
                                    Width="250"/>
                    <GridViewColumn Header="Asignat lui"
                                    DisplayMemberBinding="{Binding AssignedToUsername}"
                                    Width="250"/>
                    <GridViewColumn Header="Descriere sarcina"
                                    DisplayMemberBinding="{Binding Description}"
                                    Width="550"/>
                    <GridViewColumn Header="Status"
                                    Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.StatusValues, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                          SelectedItem="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="{Binding CanChangeStatus}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView></Border>
    </Grid>
</UserControl>
